//! Screen capture utilities for linux.

// use libspa::param::video::VideoFormat;
// use pipewire as pw;
// use std::os::fd::OwnedFd;

// // fn stream_inner(node_id: u32, fd: OwnedFd, formats: Vec<VideoFormat>) -> Result<(), pw::Error> {

// // }
